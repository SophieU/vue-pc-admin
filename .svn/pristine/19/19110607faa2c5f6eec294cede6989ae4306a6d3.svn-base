import Cookies from 'js-cookie';

const user = {
  state:{},
  mutations:{
    logout(state,vm){
      Cookie.remove('user');
      Cookie.remove('password');
      Cookie.remove('access');
    }
  }
};
export default user;
